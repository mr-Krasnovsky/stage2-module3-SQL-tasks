SELECT s.* FROM SUBJECT s JOIN MARK m ON s.ID = m.SUBJECT_ID GROUP BY SUBJECT_ID HAVING AVG(MARK) > SELECT AVG(m.MARK) FROM MARK ;
SELECT s.* FROM STUDENT s JOIN PAYMENT p ON s.ID = p.STUDENT_ID GROUP BY p.STUDENT_ID HAVING AVG (p.AMOUNT) < SELECT AVG(AMOUNT ) FROM PAYMENT;